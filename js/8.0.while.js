var _cant_ = prompt('Ingrese cantidad de numeros:');
_cant_ = parseInt(_cant_);

var contador = 0;

// Se ejecutara siempre q cumpla la condicion
while (contador < _cant_) {

    console.log('Iteración #' + contador);
    contador += 1;

}
