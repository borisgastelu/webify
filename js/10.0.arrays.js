// Arrays: coleccion de elementos en una misma posicion de memoria ordenados secuencialmente
//  Todos los arrays empiezan desde el indice 0 (posicion en la lista)

var frutas = ['manzana', 'platano', 'naranja'];     // 3 elementos en el array
console.log(frutas[0]);
console.log(frutas[1]);
console.log(frutas[2]);

var numeros = [1, 2, 3, 4, 5];  // array de numbers
console.log(numeros);

var lista = ['hola', 1, 25.51, 'mundo', 'cruel'];   // array de diversos tipos
// console.log(lista);

var objetos = ['pantalla', 'teclado', 'parlante'];
console.log(objetos);

var number = 1;
var string = 'Hola';
var boolean = true;

var tiposDatos = [number, string, boolean];
console.log(tiposDatos);
